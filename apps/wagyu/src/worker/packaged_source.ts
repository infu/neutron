/*
 * The Wagyu build replaces this module with the exact, fully bundled Worker
 * program. Keeping the source empty outside that build makes accidental use
 * from an unbundled/test runtime fail closed.
 */
const packagedWagyuVerificationWorkerSource = "";

export default packagedWagyuVerificationWorkerSource;
