/**
 * Replaced by the Files inline-worker build plugin.
 *
 * Keeping the checked-in module inert lets unit tests import the client
 * without silently constructing an unreviewed fallback worker.
 */
export const FILES_CRYPTO_WORKER_SOURCE = "";
