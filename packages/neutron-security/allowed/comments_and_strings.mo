module {
  // Prim.actorOfPrincipal and (with cycles = 10) are documentation here.
  /* nested /* Prim.regionGrow */ Prim.stableMemoryGrow */
  public let help = "actor shared <system> Prim.call_raw (context with)";
  public let marker = 'w';
}
